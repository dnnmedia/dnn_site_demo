
6TO5 = ./node_modules/.bin/6to5
MOCHA = ./node_modules/.bin/mocha

node: lib
	@mkdir -p node
	@for path in lib/*; do \
		file=`basename $$path`; \
		$(6TO5) "lib/$$file" > "node/$$file"; \
	done

test:
	@$(MOCHA)

.PHONY: test
