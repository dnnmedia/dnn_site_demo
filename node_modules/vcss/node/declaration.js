"use strict";

var _inherits = function (child, parent) {
  child.prototype = Object.create(parent && parent.prototype, {
    constructor: {
      value: child,
      enumerable: false,
      writable: true,
      configurable: true
    }
  });
  if (parent) child.__proto__ = parent;
};

var _interopRequire = function (obj) {
  return obj && (obj["default"] || obj);
};

var Block = _interopRequire(require("./block"));

var Declaration = function Declaration(sel, props) {
  this.sel = sel;
  this.props = props;
};

_inherits(Declaration, Block);

Declaration.prototype.serialize = function () {
  var props = "";
  for (var i in props) {
    if (props.length) props += "\n";
    props += "" + i + ": " + props[i];
  }
  return "" + this.sel + " {\n  " + props + "\n}";
};

module.exports = Declaration;

